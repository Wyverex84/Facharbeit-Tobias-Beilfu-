import com.github.rccookie.greenfoot.core.Map;

public class ShowMapLoader extends Map.MapLoader {

    // Just ignore this class... (but don't delete it!)

    public ShowMapLoader() {
        super(ShowMap::new, true);
    }
}
